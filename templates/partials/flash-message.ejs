<% 

if (locals.messages) { %>
    <div class="container messages">
        <% Object.keys(messages).forEach((key) => { %> <%
        messages[key].forEach((message) => { %>
        <p class="<%= key %>"><%= message %></p><br>
        <% }) %> <% }) %>
      </>
    </div>
    <% } %>
    